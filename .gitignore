__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# your journal and your bankroll are yours
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
pmbot.config.json

# cached API responses
.cache/

# fetched game logs are yours, not the repo's -- run `pmbot fetch` to build them.
# The synthetic fixture under data/sample/ is checked in on purpose.
/data/gamelogs/*
!/data/gamelogs/.gitkeep
